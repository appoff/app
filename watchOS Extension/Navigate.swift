import SwiftUI
import Offline

struct Navigate: View {
    let schema: Schema
    
    var body: some View {
        VStack {
            
        }
        .edgesIgnoringSafeArea(.all)
        .navigationBarHidden(true)
    }
}
