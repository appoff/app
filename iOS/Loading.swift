import SwiftUI
import Offline

struct Loading: View {
    let session: Session
    let factory: Factory
    
    var body: some View {
        VStack {
            
            Spacer()
        }
    }
}
