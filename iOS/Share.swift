import SwiftUI
import Offline

struct Share: View {
    let project: Project
    
    var body: some View {
        VStack {
            Button {
                
            } label: {
                HStack {
                    Text("Offload")
                    Image(systemName: "icloud.and.arrow.up")
                }
            }
        }
    }
}
