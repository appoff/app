import SwiftUI
import Offline

extension Premium {
    struct Offload: View {
        let header: Header
        
        var body: some View {
            VStack {
                Spacer()
            }
        }
    }
}
